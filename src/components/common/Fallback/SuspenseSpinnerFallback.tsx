const SuspenseSpinnerFallback = () => (
  <div className="flex h-full flex-1 items-center justify-center">
    <span className="loading loading-spinner loading-lg text-primary" />
  </div>
);

export default SuspenseSpinnerFallback;
